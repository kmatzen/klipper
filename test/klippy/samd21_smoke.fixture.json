{
    "_comment": "Minimal fixture for the SAMD21 boot/connect smoke test. No analog_in / i2c / step_trigger entries - the test just verifies that the firmware boots through SystemInit, the TC4 timer runs, and klippy completes its identify + clocksync handshake over the SERCOM0 pty Renode exposes."
}
